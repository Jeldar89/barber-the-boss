---
import { Image } from "astro:assets";

interface Props {
  background: ImageMetadata | any;
}
const { background } = Astro.props;
---

<section class="w-full h-screen bg-transparent">
  <Image src={background} alt="Hero Image" class="object-cover w-full h-full" />
  <slot />
</section>
